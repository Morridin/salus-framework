[package]
name = "models"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = {workspace = true}
serde_json = { workspace = true }
uuid = {workspace = true}

serde = { version = "1.0.228", features = ["derive"] }
